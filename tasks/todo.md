# TODO — 2026-09-09 OVERNIGHT BATCH (Brian asleep; one commit per item; suite + one sabotage per run)

Guardrails: never touch Jackson F. / Josean I. / Joseph B.; no attorney language; no staff accounts;
no live charges; no webhook-subscription changes; Rehearsal Client 2 fixes via real routes only.

- [x] 0. DB confirmations: 0003 was still paid (Stripe delivered the 08:39 resend → 401, unlatched); re-delivered via the API retry → 200, latched, refunded, credit 0. 0002 void by Brian 08:56 (notice 08:56:55); no session to expire; engagement 6e474b1f still says deposit charged $200 (finding). Both $430 accepts produced engagements + deposit invoices.
- [x] 1. Flash renders once: one page-level slot on the client page; sweep found 4 more pages (pricing, pipeline override, portal questionnaire, task-form was two components) — fixed; guard scripts/check-flash-once.mjs in the build chain (static: per-component count; no render harness exists). Sabotage: duplicate pasted back → guard fails.
- [x] 2. Flash tells the truth: void returns the notice's real state; invoice list carries every notice (void notice, refund receipt, payment receipt, invoice email) as queued/delivered/failed/skipped with the outbox + audit row; GET /invoices/:id/sends is the send log; client page shows each line + a send-log control. Found and fixed a real bug on the way: two overlapping drains (60s lane + tick) performed the refund receipt TWICE on prod at 09:08:55 — the outbox claim is now a lease. Sabotage: outbox row read as delivered → the queued tests fail.
- [x] 3. One active engagement per (contact, service_line, period): migration 0083 (period_key, partial unique index on active/on_hold + period NOT NULL, superseded_by, quotes.change_order_of); NO backfill (guardrail) — legacy rows reported, DECISION-PENDING; send gate change_order_required carries the candidates as buttons in the builder; acceptance withdraws the old ("superseded by change order <quote>"), creates the new with the period, carries unapplied paid deposit credit by moving the deposit invoice, one audit row engagement.superseded. Sabotage (index dropped): change-order test passes, two-plain-accepts fails. DONE on prod: 6adbbab4 withdrawn via closeEngagement (system actor, audit row 09:43:10). Legacy: 4 active engagements without period — Rehearsal C. ×2 (test), BRIAN S. ×1 (test dress rehearsal), Brian S. ×1 (NOT test: Brian's real migrated lead record). None on the protected names.
- [x] 4. Ops invoice display: the card already read the same `status` column as the portal — the screenshot showed paid because the DB WAS paid (resend never landed; item 0). List now carries void_reason / voided_by (email) / voided_at / amount_refunded_cents / refunded_at; the card prints "void · reason · actor · date", "refunded · $X · date", "partially refunded $X of $Y · date" via a pure formatter (apps/internal/lib/invoice-display.ts) with its own node:test suite (internal workspace now has a test runner) + an API test with all four statuses on one client. Sabotage: actor dropped from the list → API test fails.
- [ ] 5. Date formatting sweep: one helper; Chicago for Ops, client locale for portal; ISO-T grep test.
- [ ] 6. Carryovers: a) no 90-day expiry; b) retire expired sessions; c) void template EN/ES; d) refund receipt gate check
      shown in the send path; e) dashboard net-of-refund test + fix; f) is_test=false list + rehearsal flags; g) dispute SOP.
- [ ] 7. If time: Ops client page audit as Rene — list, don't fix.
