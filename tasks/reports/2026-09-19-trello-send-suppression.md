# trello-send-suppression (2026-09-19)

Generated 2026-09-19T23:41:14.683Z by scripts/report-table.mjs from the log send-suppression.log; 6 row(s).

Item 15. ZERO OUTBOX ROWS, and there is NO SUPPRESSION MECHANISM — that is the finding, stated plainly. Nothing in the import path is gated for import: enqueueEffect() in apps/api/src/outbox.ts is not automation-gated (the gate is in the drain's handler, by design: 'every row here was enqueued by a decision that already passed its own gate'), there is no import actor, no flag, and transitionStage has no path that bypasses its effects. WHAT THE ZERO ACTUALLY PROVES: the only outward hook on this path is invoiceForFiledEngagement, called by transitionStage when a return enters 'filed'. It enqueues invoice.send ONLY when final_fee_cents is set. A Trello card carries no fee, so all 5 rehearsed returns took the other branch — a task of type invoice_needed to the preparer role, which is why the task count moved 646 to 651 and the invoice count stayed at 0. The zero is a consequence of the imported data having no price, NOT of a guard. Give an imported return a final fee and the same walk enqueues one invoice.send per return to a live client. Phase 2 needs a real import mode (an actor or a flag that invoiceForFiledEngagement checks) before any row carries money. TWO FURTHER FINDINGS from the same run. (1) PHASE A, the honest import with nothing stamped, could not get past 'scheduled': all 5 returns were refused entering documents_requested with engagement_letter_required. An import cannot place a return at an accepted stage without inventing an engagement-letter signature, an estimate lock, an 8879 scan and a PTIN holder — phase B stamped all four on the throwaway copy purely so the count at 'filed' would be real. (2) transitionStage moved all 5 returns filed -> completed with federal_accepted_on, state_accepted_on and efile_accepted_at ALL NULL: R2's 'completes when every declared jurisdiction is accepted' is enforced inside recordEfileResult, not in the stage machine, so the machine will complete a return nobody has been acknowledged for. AUDIT COVERAGE: 50 audit rows, every one labelled 'Brian Soto (trello import phase-1 rehearsal 2026-09-19, applied by script)', under a session whose user_agent is 'script: trello import phase-1 rehearsal 2026-09-19, applied by script' (createSession appliedBy). RERUN: the same 114 file-02 rows offered twice created 114 tasks and then 0, on createTask's own (source_type, source_id) dedupe. The outbox was never drained; the copy holds the same 6 pre-existing rows it started with and is dropped.

```sql
REHEARSE_GATES=1 DATABASE_URL=postgresql://saos:***@postgres:5432/saos_trello_copy node --experimental-strip-types scripts/trello-import.ts --dir /trello_import --limit 114   (apps/api/scripts/trello-import.ts, run inside saos-api-1 against the pg_dump copy saos_trello_copy; the same command against database 'saos' prints: refusing: the database is 'saos'. This script runs against a COPY only)
```

| moment | outbox rows | outbox pending or failed | audit rows | task rows | notes |
|---|---|---|---|---|---|
| before the import | 6 | 0 | 17944 | 646 | 7 client-acting automations armed on the copy: attachment_acks efile_acknowledgment payment_receipt portal_upload_acks refund_receipt schedule_added_notice void_notice |
| after phase A (5 engagement(s) + return(s) created, 0 refused, no gate stamped) | 6 | 0 | 17959 | 646 | every walk stopped at a compliance gate; outbox delta 0 |
| after phase B (gates stamped, 5 reached filed, 5 reached completed) | 6 | 0 | 17999 | 651 | outbox delta 0; filed is the only hook on this path that can enqueue |
| after file 02 through createTask (114 row(s)) | 6 | 0 | 17999 | 765 | outbox delta 0; 19 to the CEO, 95 to comms_billing |
| after a rerun of the same file 02 rows | 6 | 0 | 17999 | 765 | task delta 0 |
| TOTAL DELTA | 0 | 0 | 55 | 119 | outbox effects at the end: invoice.refund_receipt=1 invoice.send=3 invoice.void_notice=2 |
