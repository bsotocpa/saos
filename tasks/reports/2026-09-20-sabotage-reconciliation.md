# sabotage-reconciliation (2026-09-20)

Generated 2026-09-20T04:45:36.995Z by scripts/report-table.mjs from the log sab-table.log; 10 row(s).

```sql
node scripts/sabotage-table.mjs 2026-09-20  (the last row per item of tasks/sabotage/2026-09-20.log, written by scripts/sabotage-run.mjs)
```

| item | file | change | test | on the harness | red | restored green |
|---|---|---|---|---|---|---|
| R8 guard: a raw INSERT INTO tasks in the path the old guard exempted | apps/api/src/migration/execute.ts | a raw `INSERT INTO tasks` appended to a migration module — the exact path check-role-guarded-tasks.mjs exempts by name, which is where the deleted Trello importer lived | guard: check:task-inserts | no | RED as expected (1 failed: apps/api/src/migration/execute.ts:225 raw INSERT INTO tasks outside the one door) | green (1 passed) |
| R9 guard: a task type emitted from apps/api/scripts with no SOP decision | apps/api/scripts/import-legacy.ts | a new `sourceType: 'legacy_import_review'` appended to a CLI script — the root the SOP-hook guard did not read until R9 | guard: check:sops | no | RED as expected (1 failed: legacy_import_review — emitted in apps/api/scripts/import-legacy.ts:110) | green (1 passed) |
| R17 harness taps: the Void control removed from the client page | apps/internal/app/clients/[id]/page.tsx | the whole sent-or-overdue Void fragment replaced by nothing; D1 cannot tap a control that is not there | harness: tests/ops-billing-taps.spec.ts | yes | RED as expected (2 failed: D1: the CEO voids a sent invoice and the row says why and who (1.5m) [phone]; D1: the CEO voids a sent invoice and the row says why and who (1.5m) [desk]) | green (6 passed) |
| ruling 10 engagement letter: the portal stamp removed | apps/api/src/modules/engagements/packet.ts | the master signature stamps no return, so a client who signed the packet in the portal still has every return blocked at Scheduled | api: test/return-controls.spec.ts | no | RED as expected (1 failed: the packet signature in the portal stamps the engagement letter on every return the client has) | green (20 passed) |
| ruling 11 preparer assignment: the preparer_required gate made unreachable | apps/api/src/modules/tax/pipeline.ts | the gate branch never taken, so preparation starts on a return assigned to nobody | api: test/return-controls.spec.ts | no | RED as expected (1 failed: preparation is refused until a preparer is named; the assign route refuses an inactive or wrong-role choice) | green (20 passed) |
| ruling 12 extension: the future-date refusal made unreachable | apps/api/src/modules/tax/extension.ts | the filed-date-in-future branch never taken, so an extension recorded as filed tomorrow buys a deadline off a filing nobody has made | api: test/return-controls.spec.ts | no | RED as expected (1 failed: recording an extension: the form, the day it was filed, and a derived deadline nobody types) | green (20 passed) |
| ruling 13 quoted range: the accepted quote's lines ignored, the base line alone | apps/api/src/modules/tax/routes.ts | the accepted quote's summed lines discarded, so the range falls back to the base return item and every quote with a schedule on it reads low | api: test/return-controls.spec.ts | no | RED as expected (3 failed: the quoted range covers the accepted quote’s schedules, and a fee equal to the quoted scope is inside it; a ranged line widens the quoted range at both ends; a pass-through is never part of the fee; role proof) | green (20 passed) |
| ruling 15 paper filing: a recorded mailing no longer satisfies its jurisdiction | apps/api/src/modules/tax/pipeline.ts | the satisfied filter reads accepted_on for every row, so a paper jurisdiction waits on an acknowledgment that never comes and the return never completes | api: test/completion.spec.ts | no | RED as expected (3 failed: a return declared federal e-file + IL paper completes on the federal acknowledgment plus the IL mailing; an old year defaults every jurisdiction to paper and completes on the mailings alone; role proof) | green (15 passed) |
| R14 the duplicate check: the query parameters transposed, so nothing is ever a duplicate | apps/api/src/modules/crm/routes.ts | GET /contacts/duplicate-check binds [email, phone, name] to $1/$2/$3, so the name is compared against an email and the check answers nothing | harness: tests/ops-add-client.spec.ts | yes | RED as expected (2 failed: the CEO adds a synthetic person, then types the same person again and reads the duplicate before anything is created (18.4s) [phone]; the CEO adds a synthetic person, then types the same person again and reads the duplicate before anything is created (16.4s) [desk]) | green (4 passed) |
| R16 import mode: the refusal in enqueueEffect made unreachable | apps/api/src/outbox.ts | the import-context branch in enqueueEffect never taken, so a client-facing effect enqueued during an import is written as a real pending row and the drain will send it | api: test/import-mode.spec.ts | no | RED as expected (4 failed: R16: an enqueue UNDER the import context is refused — no row — and audited by name; R16: the refusal reaches an enqueue several frames down, which is why it is ambient; R16: the context does not leak — after it returns, the same enqueue flows) | green (13 passed) |
