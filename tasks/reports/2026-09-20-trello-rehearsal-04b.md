# trello-rehearsal-04b (2026-09-20)

Generated 2026-09-20T07:09:59.314Z by scripts/report-table.mjs from the log import-rehearsal.log; 13 row(s).

Rulings R20-R24, rehearsed against the v2 bundle on a fresh copy of production, in a labelled script session ("trello import rehearsal 2026-09-20 (bundle 2026-09-19), applied by script"). Production before and after: 18094 audit rows, 864 contacts — identical.

R21. 04b IS THE IMPORT SOURCE FOR SERVICE FACTS AND FILE 04 IS MATCHING ONLY. The change is the KEY. File 04 is one row per business NAME, so its only identity was match_key — and 355 of its 464 rows carried no card id, which is why last night 50 rows with a live service had to be REFUSED: no key, no way to tell a second run from the first. 04b is one row per FACT, each with its own trello_source_id, and all 50 now import. The idempotency key is (source, trello_source_id, fact_type) as a PRIMARY KEY on service_fact_imports (migration 0114) — a LEDGER, not columns on each fact table, because one source row is not one fact row: an access row carrying two facts becomes two business_access_facts rows, a bookkeeping row writes a COLUMN on businesses, and an anniversary row writes a third table. A unique index on any one of those would have enforced "one fact per source row", which is not what the bundle contains. 240 source rows applied, 266 fact rows written: bookkeeping 109 applied / 130 written, annual_report_entity 56 / 48, annual_report_anniversary 42 / 42, access 33 / 46. match_key is in the ledger for reading only and is deliberately not part of the key.

AND 216 ROWS WERE READ AND DELIBERATELY NOT APPLIED, with NO ledger row, which is the one judgement call in R21 worth reading twice. sales_tax (55) and payroll (67) want engagements.filing_frequency and payroll_provider, and this import creates no bookkeeping, sales-tax or payroll engagements to hang them on; svc_1099_ty2025 (55), svc_2553 (19) and bookkeeping_status_only (20) have no column at all. Writing a ledger row for those would mark them applied forever, so the day a service-line import arrives they would be skipped in silence. They are DEFERRED and stay applicable. Separately, 74 qbo_paid_by_2022_DO_NOT_IMPORT rows are REFUSED outright per Brian, not deferred — 0 in the ledger, and all 681 businesses still read qbo_paid_by = unknown. 61 of 118 anniversary rows carry a month and day but no KIND; migration 0108 refuses one without the other on purpose (the state rule picks between an admission and an incorporation anniversary, and the wrong kind produces the wrong due date with full confidence), so those are deferred rather than guessed.

R22. THE PREPARER. All 44 imported returns took the sole active tax_preparer as their default via applyNewReturnDefaults; the query "imported returns WITHOUT a preparer" returned 0, against last night’s 44. That closes the finding I reported yesterday. THE ORDER IS LOAD-BEARING and is asserted in the spec: the defaults run AFTER setImportedStage, because the defaults inherit a letter the client ACTUALLY signed and setImportedStage refuses a return already carrying a gate fact — defaults first would make the import refuse itself on exactly the clients furthest along. THE REFUSAL was proven on the copy by deactivating the preparer: the import stopped with code no_active_tax_preparer and wrote nothing (44 returns and 240 ledger rows unchanged). My first attempt at that proof was MASKED — the import refused for a different reason, because matching had not run yet — and a refusal that is not the one under test is not a proof, so it was re-run in the right order. Several active preparers is NOT a refusal: the firm chooses, and the count is reported.

R23. AT OR PAST FILED, 10 RETURNS, BY SHAPE. 5 "accepted, client not yet notified" and 5 "paper filed" imported as COMPLETED under the attestation. Paper-filed moved from filed to completed this round, and the reason is that no acceptance can ever be RECORDED for a paper return — ack rows come only from the ATX report upload — so a paper return left at filed waits for something that cannot arrive. Nothing was invented on the way: acceptance rows on imported returns 0, mailings on imported returns 0, jurisdiction rows on those 10 returns 0, filed_date null. The 5 not-notified cards each carry a comms_billing task to tell the client by hand. "filed, awaiting ack" is implemented — R2 address default, method e-file, every row flagged declared_by_import_default, plus a preparer task to confirm against ATX — and ZERO rows in this bundle carry that value, so the shape is unexercised here and asserted in the spec instead. One refusal inside it: a tax year in the paper lane is refused rather than routed to e-file, because CLAUDE.md forbids that and a ruling does not outrank a hard rule.

R20. FIVE TASK TYPES, FIVE SOP PAGES, AND THE PEN IS EMPTY. 128 imported tasks, every one carrying an SOP link: trello_ar_worklist 114, trello_amendment 8, trello_notify_client 5, trello_books_review 1. The A/R page says what Brian dictated, including the two rules under it — never create a SAOS invoice for a return the legacy system already invoiced, and never chase a client on the amount written on the Trello card. trello_confirm_jurisdictions is registered with its page and emitted 0 times here, for the same reason as above. The PROPOSED pen in scripts/check-task-sop-hooks.mjs is now empty.

A DEFECT THIS ROUND FOUND, and 0111 had predicted it in writing. Nine cards appear in BOTH file 01 and file 02: a card reading "accepted, client not yet notified" earns a notify task from 01 and an A/R worklist task from 02 — two pieces of work, two people, one card — and 0111’s unique index on tasks (source, trello_card_id) refused the second with a constraint violation mid-import. 0111’s own header had said "a future import that wants… THREE engagements from one card cannot use this key as-is: that import needs its own discriminator, and it will have to say so rather than discover it." Migration 0114 replaces it with (source, trello_card_id, source_type): a card may produce at most one task of each KIND, which is what createTask’s own (source_type, source_id) dedupe already enforces, so the index now agrees with the door instead of being stricter than it. The other four tables keep the original key.

THE SECOND PASS CREATED NOTHING: contacts 0, businesses 0, engagements 0, returns 0, tasks 0, attestations 0, service facts 0, ledger rows 0, outbox 0. R16 still holds on top of all of it — 0 imported returns carry any gate fact, 44 attestation audit rows for 44 returns, and the import-mode probe measured 0 outbox rows under the mode against 1 with it off.

A NOTE ON THE COLUMN TOTALS: file 01 and 02 rows pass through two loops (the return loop and the missing-person loop), so "refused" and "skipped" are per-file totals across both rather than a partition of the row count. File 04’s 464 deferred rows are deferred BY DESIGN — it is matching only now.

```sql
TRELLO_IMPORT_DIR=/trello_import_v2/trello_import DATABASE_URL=postgresql://saos:***@postgres:5432/saos_trello_copy node --experimental-strip-types scripts/trello-import.ts --unsafe-no-import-mode   (run inside a THROWAWAY container built from the deployed saos-api image — never saos-api-1 — against saos_trello_copy, a fresh pg_dump of production carrying migrations 0105-0114. The scripts refuse any database whose name does not end in _copy. The copy was dropped and the bundle removed from the box at the end; production was read and never written.)
```

| source file | rows | records created | returns created | attested at a stage | tasks | service fact rows written | deferred (no home in SAOS yet) | refused or never imported | skipped (already imported, or no unique match) | second pass |
|---|---|---|---|---|---|---|---|---|---|---|
| 01_tax_wip.csv | 100 | 21 | 44 | 44 | 14 | 0 | 0 | 1 | 47 | engagements 0, returns 0, attestations 0 |
| 02_tax_ar_worklist.csv | 114 | 14 | 0 | 0 | 114 | 0 | 0 | 1 | 1 | tasks 0 |
| 03_tax_completed_roster.csv | 388 | 0 | 0 | 0 | 0 | 0 | 0 | 40 | 0 | never imported: the script refuses it in code, and its 40 missing row(s) go to out/enrichment_file03.csv and are not created |
| 04_business_services.csv | 464 | 0 | 0 | 0 | 0 | 0 | 464 | 0 | 0 | R21: matching only — every one of its 464 rows is deferred here by design, and no fact is read from it |
| 04b_service_facts.csv | 641 | 38 | 0 | 0 | 0 | 266 | 248 | 74 | 79 | businesses 0, service facts 0, ledger rows 0 |
| ALL FILES | 1707 | 73 | 44 | 44 | 128 | 266 | 712 | 116 | 127 | every delta on the second pass: contacts 0, businesses 0, engagements 0, returns 0, tasks 0, attestations 0, facts 0, ledger 0, outbox 0 |
| R22 THE PREPARER DEFAULT | 1 active tax_preparer(s) | - | 44 return(s) took the sole active preparer | - | 0 inherited a standing engagement letter | - | - | - | - | the import refuses outright when none is active; that refusal is in the sabotage manifest and in test/trello-import.spec.ts |
| R23 AT OR PAST FILED | 10 return(s) | - | - | - | - | - | - | - | - | filed-awaiting-ack 0 (jurisdictions by address default, e-file, flagged, + a confirm task); accepted-not-notified 5 (completed, no acceptance row, + a notify task); paper-filed 5 (completed, no acceptance and no mailing invented) |
| REFUSED CLIENT SENDS (audited, both passes) | - | - | - | - | - | - | - | 1 | - | outbox rows the import itself added: 0 |
| THE 8879 GATE ON AN IMPORTED ready_to_file RETURN | - | - | - | - | - | - | - | - | - | refused: engagement_letter_required — Blocked: the engagement letter is not signed. Work cannot advance past Scheduled until it is (MP compliance gate). |
| TASKS BY TYPE (on the copy, from the tasks table) | 128 | - | - | - | - | - | - | - | - | trello_amendment 8, trello_ar_worklist 114, trello_books_review 1, trello_notify_client 5 |
| 04b SERVICE FACTS BY TYPE (from the ledger) | 240 source row(s) applied | - | - | - | - | 266 | - | - | - | access: 33 applied, 46 row(s) written; annual_report_anniversary: 42 applied, 42 row(s) written; annual_report_entity: 56 applied, 48 row(s) written; bookkeeping: 109 applied, 130 row(s) written |
| 04b DEFERRED BY TYPE (read, no home in SAOS yet) | 216 | - | - | - | - | - | 216 | - | - | bookkeeping_status_only 20, payroll 67, sales_tax 55, svc_1099_ty2025 55, svc_2553 19; plus qbo_paid_by_2022_DO_NOT_IMPORT 74 REFUSED outright (Brian ruled the 2022 values out) |
