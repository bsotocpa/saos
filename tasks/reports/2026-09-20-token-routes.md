# token-routes (2026-09-20)

Generated 2026-09-20T21:45:38.939Z by scripts/report-table.mjs from the log token-routes.log; 10 row(s).

Every link a client is emailed and what a bare GET does to it; citations re-found in the source on each run (NOT FOUND would mark a moved registration).

```sql
node scripts/token-routes.mjs
```

| link | portal page | fetched on page load | marks used / changes state | single use | bare GET consumes | bare GET side effect | where |
|---|---|---|---|---|---|---|---|
| Sign-in (magic) link /auth/verify?token= | apps/portal/app/auth/verify/page.tsx (public) | POST /portal/auth/magic/verify (the page POSTs the token as soon as it mounts) | that same POST: UPDATE magic_link_tokens SET used_at WHERE used_at IS NULL AND expires_at > now() | yes (used_at), and expires after MAGIC_LINK_TTL_MINUTES | no for a plain fetch of the page; YES for a scanner that executes the page JavaScript (the mount POSTs) | none server-side from the HTML GET itself | apps/api/src/modules/portal-auth/routes.ts:65; apps/api/src/modules/portal-auth/service.ts:155; apps/portal/app/auth/verify/page.tsx:23 |
| Proposal /quote/:token | apps/portal/app/quote/[token]/page.tsx (public; no session gate on the page) | GET /public/quote/:token (read by SHA-256 of the token; 404 when unknown; returns expired as a flag) | POST /public/quote/:token/accept or /decline (status change; the hash stays valid until then) | no: reusable until accepted, declined or expires_at; the quotes table has no viewed/consumed column | no | none (a read; the only audit row is written on accept/decline) | apps/api/src/modules/pricing/quote-routes.ts:286; apps/api/src/modules/pricing/quotes.ts:431; apps/portal/app/quote/[token]/page.tsx:88 |
| Pay link /pay/:token | apps/portal/app/pay/[token]/page.tsx (public) | GET /public/pay/:token (read by pay_token_hash; expiry and revocation columns on the invoice) | POST /public/pay/:token/checkout (creates the Stripe session) and /reconcile | no: reusable until pay_token_expires_at / pay_token_revoked_at | no | none | apps/api/src/modules/billing/routes.ts:417; apps/api/src/modules/billing/pay-link.ts:100; apps/portal/app/pay/[token]/page.tsx:33 |
| Unsubscribe /unsubscribe/:id/:token | apps/portal/app/unsubscribe/[id]/[token]/page.tsx (public) | POST /public/unsubscribe/:id/:token from the page; the API ALSO serves GET with the same handler | either verb: UPDATE contacts SET broadcast_opt_out_at (HMAC token, idempotent) | no: the token is an HMAC of the contact id, never stored, never expires | not consumed (idempotent), but | YES: a bare GET opts the contact out of broadcasts (CAN-SPAM one-click, by design: "some mail clients will only follow a link") | apps/api/src/modules/comms/broadcast-routes.ts:141; apps/api/src/modules/comms/broadcast.ts:401; apps/portal/app/unsubscribe/[id]/[token]/page.tsx:21 |
| Hilo transition /transition?rt= | apps/portal/app/transition/page.tsx (public) | GET /public/transition?rt= (verifyScopedToken: a signed, purpose-scoped token; prefill only) | POST /public/transition/submit (converts the referral) | no: signed token, not stored; submit is guarded by the referral state | no | none (reads contact/business prefill) | apps/api/src/modules/referrals/routes.ts:100; apps/api/src/modules/referrals/service.ts:236; apps/portal/app/transition/page.tsx:53 |
| Intake resume token (form_resume) | apps/portal/app/intake/[key]/page.tsx (public) | GET /public/forms/:key (the definition, no token); the resume token travels only in a POST body | POST /public/forms/submissions/:id/resume, then /submit (409 once submitted) | no until submitted; then refused | no (there is no GET that accepts the token: "POST, not GET, so the resume token stays out of access logs") | none | apps/api/src/modules/forms/routes.ts:195; apps/portal/app/intake/[key]/page.tsx:156 |
| Engagement packet signing (emailed link goes to /sign) | apps/portal/app/sign/page.tsx (SESSION-GATED: no token in the link; a signed-out client is sent to /login) | GET /portal/packet, /portal/signature-envelopes, /portal/schedules, /portal/consents (all authenticateClient) | POST /portal/packet/sign | not a token link at all; the session is the credential | no | none; a bare GET of /sign without a session renders the sign-in request page | apps/api/src/modules/engagements/packet-routes.ts:190; apps/api/src/modules/engagements/packet-routes.ts:197; apps/api/src/outbox.ts:305 |
| Section 7216 consent (/consent) | apps/portal/app/consent/page.tsx (SESSION-GATED) | GET /portal/consents (authenticateClient) | POST /portal/consents | not a token link; the session is the credential | no | none | apps/api/src/modules/engagements/packet-routes.ts:233; apps/api/src/modules/engagements/packet-routes.ts:241 |
| Documents (portal upload links) | apps/portal/app/documents (SESSION-GATED; no public or share-token document route exists in apps/api) | GET /portal/documents (authenticateClient; every access audited) | n/a | not a token link; the session is the credential | no | none | apps/api/src/modules/documents/routes.ts:72 |
| Any portal page while a STALE signed-in marker is in localStorage | every page under apps/portal/app (the SessionProvider wraps all of them, public pages included) | GET /portal/me whenever localStorage saos_portal_authed = 1, even on /quote/:token | nothing: on 401 the API client clears the marker and sets window.location.href = /login | n/a | no | the redirect that stalled the proposal link: a 401 from /portal/me on a public page sends the browser to the sign-in request page | apps/portal/lib/api.ts:93; apps/portal/lib/session.tsx:65; apps/portal/lib/api.ts:33 |
