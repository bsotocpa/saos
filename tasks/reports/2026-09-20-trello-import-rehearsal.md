# trello-import-rehearsal (2026-09-20)

Generated 2026-09-20T02:53:08.778Z by scripts/report-table.mjs from the log import-rehearsal.log; 7 row(s).

Items c, f, g. THE REHEARSAL, on a fresh copy of production, in a labelled script session — every audit row it wrote reads "trello import rehearsal 2026-09-20 (bundle 2026-09-19), applied by script". The import ran inside runInImportContext, so every client-facing effect was refused and audited; the separate sabotage report carries that measurement.

ITEM c, NO FABRICATED FACTS. 44 returns were created and placed at their mapped stage through setImportedStage (apps/api/src/modules/tax/import.ts), which writes ONE column (stage), one stage-history row and ONE audit row per return carrying the attestation: "Steps before this stage were completed outside SAOS, per Trello card <id>, as of 2026-09-19." 44 attestation rows for 44 returns. The query in step 6 of the run asked the copy how many imported returns carry ANY gate fact — engagement letter, locked estimate, 8879 date, 8879 document, PTIN holder or filed date — and the answer was 0. The stages they landed at: documents_requested 11, in_preparation 7, pending_client_response 6, ready_to_file 5, filed 5, completed 5, internal_review 3, rejected 2.

AND THE GATES STILL HOLD. One imported return sitting at ready_to_file was asked to file. It was refused — on the copy, for the ENGAGEMENT LETTER, which is gate 1 and fires before the 8879 is looked at, because the import declined to fabricate gate 1 either. The 8879-specific half of the proof is in apps/api/test/import-mode.spec.ts, which stamps a synthetic letter and estimate and then asserts the refusal is f8879_required. Both halves are the same finding: an imported return cannot file until a wet-signed authorization is scanned and uploaded.

ITEM g, THE STAGE GAPS. 9 tasks instead of 9 returns: 8 "amendment in progress" cards to the tax_preparer (sourceType trello_amendment) because SAOS has no amendment stage and no 1040-X in the return_type enum, and 1 "awaiting CPA review of financials" card to the CEO (trello_books_review) because that is Brian reviewing the BOOKS, which internal_review — the review of the RETURN — is not. The 11 blocked-on-financials and awaiting-year-end-financials cards DID import, at documents_requested, with the blocker named in the stage note and in the audit row as a stage gap rather than a stage. Every task went through createTask and every owner through alertRecipientForRole, so an unfilled role is a recorded fact that falls back to the CEO rather than an unowned task. Both new task types are in the PROPOSED pen in scripts/check-task-sop-hooks.mjs awaiting Brian’s SOP ruling; for the amendment there is nothing to link until the 1040-X return type is ruled.

ITEM f, WHAT WAS CREATED. 14 contacts and 47 businesses with source = trello; 12 contacts and 47 businesses also carry unverified_import_source = trello, the tag migration 0103 gave businesses and 0111 gave contacts. The two untagged contacts are the sabotage probes, flagged is_test. A household row with nobody in SAOS becomes ONE contact, the first-named person, with the raw string kept in the note: creating two would invent a person the card only implies. File 03 created nothing.

THE RERUN. The second pass created nothing: contacts 0, businesses 0, engagements 0, returns 0, tasks 0, attestations 0, service facts 0, outbox 0. The guard is the database, not the script remembering — migration 0111 puts a unique partial index on (source, trello_card_id) on all five imported tables. createTask’s own (source_type, source_id) dedupe and migration 0083/0098’s one-active-engagement-per-line-and-period index are the second and third guards behind it.

SERVICE FACTS: 157 rows written across the 0105-0110 columns — 90 businesses with books_current_through (every one carrying its as-of date, which a CHECK requires), 58 business_access_facts rows, 9 entity_compliance anniversaries. NOT written, deliberately: businesses.qbo_paid_by stayed unknown on all 667 businesses (Brian’s instruction — the bundle’s values are from a 2022 pass), and engagements.filing_frequency and engagements.payroll_provider have no engagement to hang on because this rehearsal creates no bookkeeping, sales-tax or payroll engagements.

THREE THINGS I COULD NOT DO, each a finding rather than a gap in the work. (1) 355 of file 04’s 464 rows carry no bk_card_id, and 50 of those carry a LIVE service. (source, trello_card_id) is the rerun guard, so a row with no card id has no key and a second run could not tell it from the first: the import refuses them rather than inventing a key. Either the extract supplies a card id or Brian rules the name key is the key. (2) An imported return carries NO preparer. The pipeline now refuses in_preparation without one, so returns imported at in_preparation or later sit in nobody’s queue — who prepared a Trello return is not something the bundle knows, and naming somebody would be the same fabrication as a signature. (3) The 5 returns at filed and 5 at completed carry no filed_date, no declared jurisdictions and no acceptance, because the card says none of those things. Nothing re-checks a return already at completed, so this is stable rather than broken, but it is a shape Brian should see before Phase 2 runs for real.

A NOTE ON THE COLUMN TOTALS: file 01 and 02 rows pass through two loops — the return loop and the missing-person loop — so "refused" and "skipped" are per-file totals across both, not a partition of the row count. File 03’s refused count is its enrichment-list length; the script refuses the file in code.

```sql
TRELLO_IMPORT_DIR=/trello_import DATABASE_URL=postgresql://saos:***@postgres:5432/saos_trello_copy node --experimental-strip-types scripts/trello-import.ts --unsafe-no-import-mode   (run inside a THROWAWAY container built from the deployed saos-api image — never inside saos-api-1 — against saos_trello_copy, a fresh pg_dump of production carrying migrations 0105-0111. The scripts refuse any database whose name does not end in _copy. The copy was dropped and the bundle removed from the box at the end; production was read and never written.)
```

| source file | rows | records created | returns created | attested at a stage | tasks | service fact rows | refused or not importable | skipped (already imported, or no unique match) | second pass |
|---|---|---|---|---|---|---|---|---|---|
| 01_tax_wip.csv | 100 | 22 | 44 | 44 | 9 | 0 | 1 | 47 | engagements 0, returns 0, attestations 0 |
| 02_tax_ar_worklist.csv | 114 | 15 | 0 | 0 | 114 | 0 | 1 | 1 | tasks 0 |
| 03_tax_completed_roster.csv | 388 | 0 | 0 | 0 | 0 | 0 | 40 | 0 | never imported: the script refuses it in code, and its 40 missing row(s) go to out/enrichment_file03.csv and are not created |
| 04_business_services.csv | 464 | 22 | 0 | 0 | 0 | 157 | 355 | 16 | businesses 0, service facts 0 |
| ALL FILES | 1066 | 59 | 44 | 44 | 123 | 157 | 397 | 64 | every delta on the second pass: contacts 0, businesses 0, engagements 0, returns 0, tasks 0, attestations 0, facts 0, outbox 0 |
| REFUSED CLIENT SENDS (audited, both passes) | - | - | - | - | - | - | 1 | - | outbox rows the import itself added: 0 |
| THE 8879 GATE ON AN IMPORTED ready_to_file RETURN | - | - | - | - | - | - | - | - | refused: engagement_letter_required — Blocked: the engagement letter is not signed. Work cannot advance past Scheduled until it is (MP compliance gate). |
