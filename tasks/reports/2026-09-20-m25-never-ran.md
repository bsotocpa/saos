# m25-never-ran (2026-09-20)

Generated 2026-09-20T02:08:27.447Z by scripts/report-table.mjs from production; 1 row(s).

R8: what apps/api/src/migration/trello.ts (importTrelloBoard) would have written, counted on production. The importer wrote no audit row and no run record of its own, so its only traces are the rows it inserted: tasks carrying source_type = 'trello' (its idempotence key), the boards and board_columns it created from a board export's lists, the task_checklist_items it attached to those tasks, and tasks sitting on a board column. Zero across every marker means it never ran here.

```sql
SELECT (SELECT count(*) FROM tasks WHERE source_type = 'trello') AS tasks_source_type_trello, (SELECT count(*) FROM tasks WHERE source = 'import') AS tasks_source_import, (SELECT count(*) FROM boards) AS boards, (SELECT count(*) FROM board_columns) AS board_columns, (SELECT count(*) FROM tasks WHERE board_column_id IS NOT NULL) AS tasks_on_a_board_column, (SELECT count(*) FROM task_checklist_items) AS task_checklist_items, (SELECT count(*) FROM audit_log WHERE action LIKE 'trello%' OR action LIKE '%trello%') AS audit_rows_mentioning_trello, (SELECT count(*) FROM tasks) AS tasks_total
```

| tasks_source_type_trello | tasks_source_import | boards | board_columns | tasks_on_a_board_column | task_checklist_items | audit_rows_mentioning_trello | tasks_total |
|---|---|---|---|---|---|---|---|
| 0 | 0 | 0 | 0 | 0 | 0 | 0 | 646 |
