# walk-evidence-path-e (2026-09-20)

Generated 2026-09-21T01:56:26.474Z by scripts/report-table.mjs from the log walk-E.log; 8 row(s).

Path E (R37): the emailed link opens what it names, and the migrated client's two addresses. E1 was red on this run at both viewports: the spec quoted a business line for a contact with no business and the business_required rule on POST /quotes refused it (the spec's fault, fixed after the run; not rerun).

```sql
node scripts/walk-evidence.mjs E  (reads apps/e2e/.artifacts/last-run.json from the harness run of 2026-09-20 19:44-19:47, R37 on builder v2: 18 passed, 2 failed)
```

| step | what | device | control (page + selector) | roles | harness test | viewport | last run | how | cleared |
|---|---|---|---|---|---|---|---|---|---|
| E1 | A proposal link renders its quote for the recipient with a lapsed session and the browser's stale signed-in marker; no redirect to the sign-in request page | phone + laptop | portal /quote/:token at phone, followed from the emailed href with a lapsed session and the browser's stale signed-in marker: heading "Your proposal" and button "Accept and start the work" | client (the emailed proposal link) | apps/e2e/tests/portal-public-links.spec.ts:82 | phone | failed | tap | NO |
| E1 | A proposal link renders its quote for the recipient with a lapsed session and the browser's stale signed-in marker; no redirect to the sign-in request page | phone + laptop | portal /quote/:token at desk, followed from the emailed href with a lapsed session and the browser's stale signed-in marker: heading "Your proposal" and button "Accept and start the work" | client (the emailed proposal link) | apps/e2e/tests/portal-public-links.spec.ts:82 | desk | failed | tap | NO |
| E2 | A sign-in link is spent by the button on its page, never by the load | phone + laptop | portal /auth/verify?token= at phone, followed from the emailed href: the load leaves used_at null, button "Sign in" spends the link and signs the client in | client (the emailed sign-in link) | apps/e2e/tests/portal-public-links.spec.ts:133 | phone | passed | tap | yes |
| E2 | A sign-in link is spent by the button on its page, never by the load | phone + laptop | portal /auth/verify?token= at desk, followed from the emailed href: the load leaves used_at null, button "Sign in" spends the link and signs the client in | client (the emailed sign-in link) | apps/e2e/tests/portal-public-links.spec.ts:133 | desk | passed | tap | yes |
| E3 | The client page warns when the portal sign-in address is not the contact email and offers one control to make them the same; refused in the server's words when another account holds it; the bookkeeper has no control | phone + laptop | /clients/:id Portal access line, button "Use the contact email for sign-in" → ask() modal, button "Use the contact email" at phone: the warning line, the refusal in the server's words when another account holds the address, then the alignment and the warning gone | ceo, comms_billing (contacts.write) | apps/e2e/tests/ops-portal-email.spec.ts:71 | phone | passed | tap | yes |
| E3 | The client page warns when the portal sign-in address is not the contact email and offers one control to make them the same; refused in the server's words when another account holds it; the bookkeeper has no control | phone + laptop | role proof: bookkeeper sees the warning and no "Use the contact email for sign-in" control, POST /contacts/:id/align-portal-email refused 403 | ceo, comms_billing (contacts.write) | apps/e2e/tests/ops-portal-email.spec.ts:121 | phone | passed | tap | yes |
| E3 | The client page warns when the portal sign-in address is not the contact email and offers one control to make them the same; refused in the server's words when another account holds it; the bookkeeper has no control | phone + laptop | /clients/:id Portal access line, button "Use the contact email for sign-in" → ask() modal, button "Use the contact email" at desk: the warning line, the refusal in the server's words when another account holds the address, then the alignment and the warning gone | ceo, comms_billing (contacts.write) | apps/e2e/tests/ops-portal-email.spec.ts:71 | desk | passed | tap | yes |
| E3 | The client page warns when the portal sign-in address is not the contact email and offers one control to make them the same; refused in the server's words when another account holds it; the bookkeeper has no control | phone + laptop | role proof: bookkeeper sees the warning and no "Use the contact email for sign-in" control, POST /contacts/:id/align-portal-email refused 403 | ceo, comms_billing (contacts.write) | apps/e2e/tests/ops-portal-email.spec.ts:121 | desk | passed | tap | yes |
