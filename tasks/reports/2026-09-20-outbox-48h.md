# outbox-48h (2026-09-20)

Generated 2026-09-20T21:43:47.583Z by scripts/report-table.mjs from production; 1 row(s).

Outbox rows of the last 48 hours by effect and status. The sign-in link and the proposal email do not go through the outbox (sendTemplatedEmail sends inline), so their absence here is expected.

```sql
SELECT effect, status::text, count(*) AS rows, max(attempts) AS max_attempts, count(sent_at) AS sent FROM outbox WHERE created_at > now() - interval '48 hours' GROUP BY 1,2 ORDER BY 1,2
```

| effect | status | rows | max_attempts | sent |
|---|---|---|---|---|
| packet.send_signature_link | sent | 1 | 1 | 1 |
