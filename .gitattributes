# Normalize to LF in the repo and on checkout — this project deploys to Linux
# (Docker), and CRLF in shell scripts or compose files breaks silently.
* text=auto eol=lf

# Binaries
*.pdf binary
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
