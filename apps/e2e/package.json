{
  "name": "@saos/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Rendered-output harness: the real API, the real Ops app, a real browser, one page per run (decision 3, 2026-09-10).",
  "type": "module",
  "scripts": {
    "test": "node prune-artifacts.mjs && playwright test",
    "test:e2e": "node prune-artifacts.mjs && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
